<!DOCTYPE html>
<html>
<head>
    <title>Kestvuskatsed</title>
</head>
<body>
    <h1>Kestvuskatsed</h1>

    <h2>Lisa uus pudel</h2>
    <form method="post" action="/add">
        Toode:<br>
        <input type="text" name="product" required><br><br>

        Pudel ID või number:<br>
        <input type="text" name="bottle_id" required><br><br>

        Villimise kuupäev:<br>
        <input type="date" name="bottled_date" required><br><br>

        Kontrolli mitme kuu pärast:<br>
        <input type="number" name="interval_months" required><br><br>

        <button type="submit">Lisa pudel</button>
    </form>

    <h2>Maitsmed järjekorras</h2>

    <table border="1" cellpadding="5">
        <tr>
            <th>Toode</th>
            <th>Pudel</th>
            <th>Villitud</th>
            <th>Järgmine maitsmine</th>
            <th>Staatus</th>
            <th>Tulemus</th>
            <th>Järeldus</th>
            <th>Tegevus</th>
        </tr>

        {% for item in data %}
        <tr>
            <td>{{ item.product }}</td>
            <td>{{ item.bottle_id }}</td>
            <td>{{ item.bottled_date }}</td>
            <td>{{ item.next_tasting }}</td>
            <td>{{ item.status }}</td>
            <td>{{ item.result }}</td>
            <td>{{ item.conclusion }}</td>
            <td>
                {% if item.status == "ootab" %}
                <form method="post" action="/taste/{{ item.id }}">
                    Tulemus:<br>
                    <input type="text" name="result" required><br>
                    Järeldus:<br>
                    <input type="text" name="conclusion" required><br>
                    <button type="submit">Märgista maitsituks</button>
                </form>
                {% else %}
                -
                {% endif %}
                <br>
                <a href="/delete/{{ item.id }}">Kustuta</a>
            </td>
        </tr>
        {% endfor %}
    </table>

</body>
</html>